<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.wegrid.board.mapper.BoardMapper">

    <insert id="insertBoardAttachment">
        INSERT ALL
        <foreach collection="attachmentVoList" item = "x">
            INTO BOARD_ATTACHMENT (NO, BOARD_NO, ORIGIN_NAME, CHANGE_NAME) VALUES ((SELECT GET_BOARD_ATTACHMENT_NO FROM DUAL), SEQ_BOARD.CURRVAL, #{x.originName}, #{x.changeName})
        </foreach>
        SELECT 1 FROM DUAL
    </insert>

    <select id="getBoardVoList">
        SELECT
        B.NO
        , B.TITLE
        , B.CONTENT
        , B.WRITER_NO
        , B.HIT
        , B.CREATE_DATE
        , B.DEL_YN
        , M.NICK AS WRITER_NICK
        FROM BOARD B
        JOIN MEMBER M ON( B.WRITER_NO = M.NO )
        WHERE B.DEL_YN = 'N'
        <if test="searchType == 'title'">
            AND TITLE LIKE '%${searchValue}%'
        </if>
        <if test="searchType == 'content'">
            AND CONTENT LIKE '%${searchValue}%'
        </if>
        ORDER BY B.NO DESC
        OFFSET #{pvo.offset} ROWS FETCH NEXT #{pvo.boardLimit} ROWS ONLY
    </select>

    <select id="getBoardCnt">
        SELECT COUNT(NO)
        FROM BOARD
        WHERE DEL_YN ='N'
        <if test="searchType == 'title'">
            AND TITLE LIKE '%${searchValue}%'
        </if>
        <if test="searchType == 'content'">
            AND TITLE CONTENT '%${searchValue}%'
        </if>
    </select>

</mapper>